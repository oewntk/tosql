CREATE TABLE ${lexes_morphs.table}
(
    ${lexes_morphs.luid}    INTEGER      NOT NULL,
    ${lexes_morphs.wordid}  INTEGER      NOT NULL,
    ${lexes_morphs.posid}   CHARACTER(1) NOT NULL,
    ${lexes_morphs.morphid} INTEGER      NOT NULL
);
